# Maintainer: Bruno Pagani <archange@archlinux.org>
# ppc64le: Omarchy ppc64le -- see README.md

pkgname=openvkl
pkgver=2.0.2
pkgrel=1
pkgdesc="Intel® Open Volume Kernel Library"
arch=(x86_64 powerpc64le)
url="https://www.openvkl.org/"
license=(Apache-2.0)
depends=(
  embree
  libgcc
  libstdc++
  glibc
  onetbb
  openvdb
  rkcommon
)
makedepends=(boost cmake ispc)
source=(
  https://github.com/openvkl/openvkl/archive/v${pkgver}/${pkgname}-${pkgver}.tar.gz
  openvkl-ppc64le.patch
)
sha256sums=('c5c6dd9984ee527e08d726770034a9bb7f52e06e65ec0a78368a5d930a53d122'
            'SKIP')

prepare() {
  cd ${pkgname}-${pkgver}
  # ppc64le: VSX ISA / ispc --arch=ppc64le
  patch -Np1 -i "$srcdir"/openvkl-ppc64le.patch
}

build() {
  export CMAKE_POLICY_VERSION_MINIMUM=3.5

  cmake -B build -S ${pkgname}-${pkgver} \
    -DCMAKE_BUILD_TYPE=None \
    -DCMAKE_INSTALL_PREFIX=/usr \
    -DBUILD_EXAMPLES=OFF \
    -DOpenVDB_ROOT=/usr \
    -Wno-dev
  cmake --build build --verbose
}

check() {
  LD_LIBRARY_PATH="${PWD}"/build ctest --test-dir build --output-on-failure
}

package() {
  DESTDIR="${pkgdir}" cmake --install build
}
